/**
 * @description
 *
 * Represents a type.
 *
 * An example of a `Type` is `MyCustomComponent` class, which in JavaScript is be represented by
 * the `MyCustomComponent` constructor function.
 */
export declare type Type = FunctionConstructor;